import Link from "next/link"; import type {CommunityPost} from "../../lib/types";
export default function PostCard({post}:{post:CommunityPost}){return <Link href={`/community/${post.id}`} className="community-card"><div className="post-meta"><span className="avatar">{post.displayName.slice(0,1)}</span><span>{post.displayName}　{post.ageRange}</span><time>{post.createdAt}</time></div><div className="tags"><span>#{post.topic}</span></div><h2>{post.title}</h2><p>{post.body}</p><div className="card-stats"><span>♡ {post.likes}</span><span>返信 {post.comments.length}</span></div></Link>}
