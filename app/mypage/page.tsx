"use client";
import {useEffect,useState} from "react";import Link from "next/link";
export default function MyPage(){const [posts,setPosts]=useState<any[]>([]),[saves,setSaves]=useState<any[]>([]);const load=()=>{fetch("/api/me/posts").then(r=>r.ok?r.json():[]).then(setPosts);fetch("/api/me/saves").then(r=>r.ok?r.json():[]).then(setSaves)};useEffect(load,[]);const unsave=async(id:string)=>{await fetch("/api/me/saves",{method:"DELETE",headers:{"Content-Type":"application/json"},body:JSON.stringify({postId:id})});load()};return <main className="phone-shell"><section className="page-content mypage"><p className="eyebrow">MY PAGE</p><h1>マイページ</h1><div className="my-sections"><section><h2>自分の投稿</h2>{posts.length?posts.map(p=><Link key={p.id} href={`/community/${p.id}`} className="community-card"><h2>{p.title}</h2><p>{p.status==="pending"?"確認待ち":"公開中"}</p></Link>):<p>投稿した相談はここに表示されます。</p>}</section><section><h2>保存した体験談</h2>{saves.length?saves.map(s=><div className="community-card" key={s.post_id}><Link href={`/community/${s.post_id}`}><h2>{s.community_posts?.title||"体験談"}</h2></Link><button className="report-link" onClick={()=>unsave(s.post_id)}>保存を解除</button></div>):<p>保存した体験談を後から見返せます。</p>}</section><section><h2>安心して利用いただくために</h2><p>・お一人につき1アカウントでのご利用をお願いします。</p><p>・悪質な投稿、誹謗中傷、営業・勧誘などは管理者の判断で削除・利用制限を行う場合があります。</p></section></div></section></main>}
