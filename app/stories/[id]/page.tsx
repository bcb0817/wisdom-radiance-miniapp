import Link from "next/link";import {stories} from "../../../lib/mockData";
export default function StoryDetail({params}:{params:{id:string}}){const s=stories.find(x=>x.id===params.id);if(!s)return <main className="phone-shell"><section className="page-content"><h1>体験談が見つかりません</h1><Link href="/stories" className="back-link">← みんなの体験談へ戻る</Link></section></main>;return <main className="phone-shell"><section className="page-content detail-content"><Link href="/stories" className="back-link">← みんなの体験談へ戻る</Link><article className="post-detail"><div className="post-meta"><span className="avatar">{s.age}</span><span>{s.age}歳の体験</span></div><div className="tags">{s.tags.map(t=><span key={t}>#{t} </span>)}</div><h1>{s.title}</h1>{[["最初に感じた変化",s.change],["何に困ったか",s.trouble],["試したこと",s.tried.join("\n")],["本人の実感",`「${s.feeling}」`],["今振り返って思うこと",s.reflection]].map(([title,text])=><div className="safety-notice" key={title}><strong>{title}</strong><p>{text}</p></div>)}<p className="lead">※これは個人の体験談です。症状が気になる場合は医療機関へご相談ください。</p></article></section></main>}
