import Link from "next/link"; import {stories} from "../../lib/mockData";
export default function StoriesPage(){return <main className="phone-shell"><section className="page-content"><p className="eyebrow">STORIES</p><h1>体験談を探す</h1><p className="lead">同じ悩みを持つ人の経験から、自分なりのヒントを探しましょう。</p><div className="post-list">{stories.map(s=><Link key={s.id} href="/" className="community-card"><div className="post-meta"><span className="avatar">{s.age}</span><span>{s.age}歳の体験</span><time>体験談</time></div><div className="tags"><span>#{s.topic}</span></div><h2>{s.title}</h2><p>{s.intro}</p></Link>)}</div></section></main>}
