import {NextResponse} from "next/server"; import {cookies} from "next/headers"; import {createPost,listPosts} from "../../../../lib/community";
export async function GET(request:Request){try{const topic=new URL(request.url).searchParams.get("topic")||undefined;return NextResponse.json(await listPosts(topic))}catch(error){return NextResponse.json({error:String(error)},{status:500})}}
export async function POST(request:Request){try{const body=await request.json();if(!body.displayName||!body.title||!body.body||!body.topic)return NextResponse.json({error:"必須項目が不足しています"},{status:400});const lineUserId=cookies().get("wr_line_user")?.value;return NextResponse.json(await createPost({...body,lineUserId}),{status:201})}catch(error){const detail=error instanceof Error?{message:error.message}:error;return NextResponse.json({error:typeof detail==="string"?detail:JSON.stringify(detail)},{status:500})}}
