import {NextResponse} from "next/server";import {adminSupabase} from "../../../../lib/supabase/admin";
export async function GET(){if(!adminSupabase)return NextResponse.json({error:"管理用Supabaseキーが設定されていません"},{status:503});const {data,error}=await adminSupabase.from("community_reports").select("id,post_id,comment_id,reason,created_at").order("created_at",{ascending:false});if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json(data||[])}
