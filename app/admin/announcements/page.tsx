"use client";
import {useEffect,useState} from "react";import Link from "next/link";
export default function AnnouncementsAdmin(){const [items,setItems]=useState<any[]>([]),[title,setTitle]=useState(""),[body,setBody]=useState("");const load=()=>{fetch("/api/admin/announcements").then(r=>r.ok?r.json():[]).then(setItems).catch(()=>{})};useEffect(()=>{load()},[]);const create=async(e:React.FormEvent)=>{e.preventDefault();if(!title.trim()||!body.trim())return;await fetch("/api/admin/announcements",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({title,body})});setTitle("");setBody("");load()};const remove=async(id:string)=>{await fetch("/api/admin/announcements",{method:"DELETE",headers:{"Content-Type":"application/json"},body:JSON.stringify({id})});load()};return <main className="phone-shell"><section className="page-content mypage"><Link href="/admin/moderation" className="back-link">← モデレーションへ戻る</Link><p className="eyebrow">ANNOUNCEMENTS</p><h1>お知らせ管理</h1><form className="community-form" onSubmit={create}><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="タイトル"/><textarea value={body} onChange={e=>setBody(e.target.value)} rows={5} placeholder="本文"/><button className="primary-button">お知らせを公開</button></form><div className="my-sections">{items.map(item=><section key={item.id}><h2>{item.title}</h2><p>{item.body}</p><button className="report-link" onClick={()=>remove(item.id)}>削除</button></section>)}</div></section></main>}
