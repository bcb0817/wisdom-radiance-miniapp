"use client";
import {useState} from "react";import {useRouter} from "next/navigation";
export default function AdminLogin(){const [password,setPassword]=useState("");const [error,setError]=useState("");const router=useRouter();return <main className="phone-shell"><section className="page-content form-content"><p className="eyebrow">ADMIN</p><h1>運営ログイン</h1><form className="community-form" onSubmit={async e=>{e.preventDefault();const r=await fetch("/api/admin/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password})});if(r.ok)router.push("/admin/moderation");else setError("パスワードが正しくありません")}}><label>管理者パスワード<input type="password" value={password} onChange={e=>setPassword(e.target.value)} /></label>{error&&<p className="form-error">{error}</p>}<button className="primary-button">ログイン</button></form></section></main>}
