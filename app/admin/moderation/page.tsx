"use client";
import {useEffect,useState} from "react";import Link from "next/link";
export default function ModerationPage(){const [posts,setPosts]=useState<any[]>([]),[reports,setReports]=useState<any[]>([]);const load=()=>{fetch("/api/admin/posts").then(r=>r.ok?r.json():[]).then(setPosts);fetch("/api/admin/reports").then(r=>r.ok?r.json():[]).then(setReports)};useEffect(load,[]);const act=async(id:string,method:string,status?:string)=>{await fetch("/api/admin/posts",{method,headers:{"Content-Type":"application/json"},body:JSON.stringify(status?{id,status}:{id})});load()};return <main className="phone-shell"><section className="page-content mypage"><Link href="/" className="back-link">← ホームへ戻る</Link><p className="eyebrow">MODERATION</p><h1>運営モデレーション</h1><div className="my-sections"><section><h2>確認待ち（{posts.length}件）</h2>{posts.length===0?<p>確認待ちの投稿はありません。</p>:posts.map(p=><div className="moderation-row" key={p.id}><span>{p.title}</span><button className="report-link" onClick={()=>act(p.id,"PATCH","published")}>公開</button><button className="report-link" onClick={()=>act(p.id,"PATCH","rejected")}>却下</button><button className="report-link" onClick={()=>act(p.id,"DELETE")}>削除</button></div>)}</section><section><h2>通報一覧（{reports.length}件）</h2>{reports.map(r=><div className="moderation-row" key={r.id}><span>{r.comment_id?"返信":"投稿"}：{r.reason}</span></div>)}</section></div></section></main>}
