"use client";
import {useEffect,useState} from "react";import Link from "next/link";import {communityPosts} from "../../../lib/mockCommunityData";
export default function ModerationPage(){const [pending,setPending]=useState<any[]>([]),[reports,setReports]=useState<any[]>([]),[blocked,setBlocked]=useState<string[]>([]);const names=Array.from(new Set(communityPosts.flatMap(p=>[p.displayName,...p.comments.map(c=>c.displayName)])));const load=()=>{fetch("/api/admin/posts").then(r=>r.ok?r.json():[]).then(setPending);fetch("/api/admin/reports").then(r=>r.ok?r.json():[]).then(setReports);fetch("/api/admin/blocks").then(r=>r.ok?r.json():[]).then(x=>setBlocked(x.map((v:any)=>v.display_name)))};useEffect(load,[]);const review=async(id:string,status:string)=>{await fetch("/api/admin/posts",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({id,status})});load()};const block=async(name:string,on:boolean)=>{await fetch("/api/admin/blocks",{method:on?"DELETE":"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({displayName:name})});load()};return <main className="phone-shell"><section className="page-content mypage"><Link href="/" className="back-link">← ホームへ戻る</Link><p className="eyebrow">MODERATION</p><h1>運営モデレーション</h1><div className="my-sections"><section><h2>確認待ち（{pending.length}件）</h2>{pending.length===0?<p>確認待ちの投稿はありません。</p>:pending.map(p=><div className="moderation-row" key={p.id}><div><strong>{p.title}</strong><p>{p.body}</p><small>{p.display_name}　{p.topic_slug}</small></div><button className="report-link" onClick={()=>review(p.id,"published")}>公開</button><button className="report-link" onClick={()=>review(p.id,"rejected")}>却下</button></div>)}</section><section><h2>通報一覧（{reports.length}件）</h2>{reports.map(r=><div className="moderation-row" key={r.id}><span>{r.comment_id?"返信":"投稿"}：{r.reason}</span></div>)}</section><section><h2>投稿者を選択</h2>{names.map(name=><div className="moderation-row" key={name}><span>{name}</span>{blocked.includes(name)?<><span>ブロック中</span><button className="report-link" onClick={()=>block(name,true)}>解除</button></>:<button className="report-link" onClick={()=>block(name,false)}>ブロック</button>}</div>)}</section></div></section></main>}
