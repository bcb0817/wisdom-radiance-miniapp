"use client";
import {useEffect,useState} from "react";import Link from "next/link";import {communityPosts} from "../../../lib/mockCommunityData";import {blockUser,getBlockedUsers,unblockUser} from "../../../lib/moderation";
export default function ModerationPage(){const [blocked,setBlocked]=useState<string[]>([]);const [reports,setReports]=useState<any[]>([]);useEffect(()=>{setBlocked(getBlockedUsers());fetch("/api/admin/reports").then(r=>r.ok?r.json():[]).then(setReports).catch(()=>setReports([]))},[]);const names=Array.from(new Set(communityPosts.flatMap(p=>[p.displayName,...p.comments.map(c=>c.displayName)])));return <main className="phone-shell"><section className="page-content mypage"><Link href="/" className="back-link">← ホームへ戻る</Link><p className="eyebrow">MODERATION</p><h1>運営モデレーション</h1><div className="my-sections"><section><h2>通報一覧（{reports.length}件）</h2>{reports.length===0?<p>通報はありません。</p>:reports.map(r=><div className="moderation-row" key={r.id}><span>{r.comment_id?"返信":"投稿"}：{r.reason}</span><small>{new Date(r.created_at).toLocaleString("ja-JP")}</small></div>)}</section><section><h2>ブロック済み</h2>{blocked.length===0?<p>ブロック中のユーザーはいません。</p>:blocked.map(name=><div className="moderation-row" key={name}><span>{name}</span><button className="report-link" onClick={()=>{unblockUser(name);setBlocked(getBlockedUsers())}}>解除</button></div>)}</section><section><h2>投稿者を選択</h2>{names.map(name=><div className="moderation-row" key={name}><span>{name}</span>{blocked.includes(name)?<span>ブロック中</span>:<button className="report-link" onClick={()=>{blockUser(name);setBlocked(getBlockedUsers())}}>ブロック</button>}</div>)}</section></div></section></main>}
